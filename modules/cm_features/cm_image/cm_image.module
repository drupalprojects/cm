<?php

include_once('cm_image.features.inc');

function cm_image_nodeapi(&$node, $op, $a3 = NULL, $a4 = NULL) {
  if($node->type == 'image_gallery' && $a4) {
    $path = drupal_get_path('module', 'cm_image') . '/css/cm_image-gallery.css';
    drupal_add_css($path, 'module', 'all', FALSE);
  }
}
